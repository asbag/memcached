<?php
//Memcached is a open-sourced, high-performance, 
//distributed memory caching system. It is an in-memory 
//key-value store for small chunks of arbitrary data 
//(strings, objects) from results of database calls, API calls, or page rendering.
//Single Server - stored in host:port format
var_dump($_ENV['MEMCACHED_SERVERS']);
list($host, $port) = explode($_ENV['MEMCACHED_SERVERS'],':');